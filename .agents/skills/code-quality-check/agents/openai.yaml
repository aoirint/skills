interface:
  display_name: "Code Quality Check"
  short_description: "Review implementation quality and verification."
  default_prompt: >-
    Use $code-quality-check to review this implementation change for readability, maintainability,
    design intent, and verification; use $security-check for sensitive supply-chain updates.
