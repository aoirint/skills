interface:
  display_name: "AWS Workflow"
  short_description: "Create, migrate, and verify AWS CDK resources safely"
  default_prompt: "Use $aws-workflow to create or review the requested Python CDK project, AWS change, or existing-resource migration with least privilege, explicit ownership, and redacted evidence."
