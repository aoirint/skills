name: Main

on:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  lint:
    runs-on: ubuntu-slim

    steps:
      # Revalidate the merged commit without retaining credentials that lint does not need.
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # Reuse the identical dependency and lint boundary used for proposed changes.
      - name: Run Node.js lint
        uses: ./.github/actions/lint-node
