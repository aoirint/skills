interface:
  display_name: "Release Note Workflow"
  short_description: "Derive user-facing stable release notes."
  default_prompt: >-
    Use $release-note-workflow to derive stable user-facing release notes and
    check their release readiness from the canonical changelog.
