interface:
  display_name: "APM Usage"
  short_description: "Safely manage pinned APM agent dependencies"
  default_prompt: "Use $apm-usage to safely add or update this project's APM agent dependencies."
