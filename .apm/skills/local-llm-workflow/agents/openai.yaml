interface:
  display_name: "Local LLM Workflow"
  short_description: "Evaluate local LLM dependencies with evidence"
  default_prompt: "Use $local-llm-workflow to decide whether a local LLM dependency is justified and produce a bounded, evidence-backed evaluation or reference prototype without treating it as production-ready."
