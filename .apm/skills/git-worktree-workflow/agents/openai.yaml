interface:
  display_name: "Git Worktree Workflow"
  short_description: "Use Git worktrees for implementation and safe PR merges."
  default_prompt: >-
    Use $git-worktree-workflow to set up a Git worktree for this repository task, then follow the
    repository's implementation, verification, and safe pull-request merge workflow.
