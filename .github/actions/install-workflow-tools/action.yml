name: Install workflow validation tools
description: Install checksum-verified ShellCheck, actionlint, and pinact binaries.

runs:
  using: composite
  steps:
    - name: Install workflow validation tools
      shell: bash
      run: bash "${{ github.action_path }}/install-workflow-tools.sh"
